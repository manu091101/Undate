import { defineCloudflareConfig } from '@opennextjs/cloudflare';

// Original Undate Next.js app on Cloudflare Workers via OpenNext.
// No R2 cache required for MVP deploy.
export default defineCloudflareConfig({});
